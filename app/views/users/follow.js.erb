var followActivated = "user-follow-activated"
$('.user-follow').addClass(followActivated);
<% if current_user.following? @user %>
  $('.user-follow').addClass(followActivated);
<% else %>
  $('.user-follow').removeClass(followActivated);
<% end %>
